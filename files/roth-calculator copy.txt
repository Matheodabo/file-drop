<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Roth Conversion Analysis | The Vinder Group | Morgan Stanley</title>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=DM+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{--ms-navy:#00263A;--ms-blue:#003C71;--ms-mid:#00537E;--ms-accent:#0078C8;--ms-light:#4DA8DA;--ms-ice:#D4EEF7;--ms-pale:#EAF5FB;--ms-white:#FAFCFE;--ms-gold:#B8860B;--ms-gold-light:#D4A843;--text-primary:#1A2A3A;--text-secondary:#4A5A6A;--text-muted:#7A8A9A;--border:#D0D8E0;--border-light:#E8EEF2;--success:#1B8A5A;--warning:#C87800;--danger:#C83232;--card-shadow:0 2px 12px rgba(0,38,58,0.06);--card-shadow-hover:0 4px 24px rgba(0,38,58,0.10);--radius:10px;--radius-sm:6px;--transition:0.25s cubic-bezier(0.4,0,0.2,1)}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}html{scroll-behavior:smooth}
body{font-family:'DM Sans',sans-serif;background:var(--ms-white);color:var(--text-primary);line-height:1.6;-webkit-font-smoothing:antialiased}
.header{background:linear-gradient(135deg,var(--ms-navy) 0%,var(--ms-blue) 50%,var(--ms-mid) 100%);color:white;position:relative;overflow:hidden}
.header::before{content:'';position:absolute;top:-50%;right:-20%;width:600px;height:600px;background:radial-gradient(circle,rgba(255,255,255,0.04) 0%,transparent 70%);border-radius:50%}
.header-inner{max-width:1280px;margin:0 auto;padding:32px 40px 36px;position:relative;z-index:1}
.header-brand{display:flex;align-items:center;gap:16px;margin-bottom:6px}
.header-logo{width:44px;height:44px;background:rgba(255,255,255,0.12);border:1px solid rgba(255,255,255,0.2);border-radius:8px;display:flex;align-items:center;justify-content:center;font-family:'Cormorant Garamond',serif;font-weight:700;font-size:22px;color:var(--ms-gold-light)}
.header-titles h2{font-weight:500;font-size:13px;letter-spacing:2.5px;text-transform:uppercase;color:rgba(255,255,255,0.65)}
.header-titles h1{font-family:'Cormorant Garamond',serif;font-weight:600;font-size:28px;color:white}
.header-subtitle{font-size:14px;color:rgba(255,255,255,0.55);margin-top:4px;padding-left:60px;font-weight:300}
.main{max-width:1280px;margin:0 auto;padding:32px 40px 60px}
.input-grid{display:grid;grid-template-columns:1fr 1fr;gap:24px;margin-bottom:32px}
@media(max-width:900px){.input-grid{grid-template-columns:1fr}.header-inner,.main{padding-left:20px;padding-right:20px}}
.card{background:white;border:1px solid var(--border-light);border-radius:var(--radius);box-shadow:var(--card-shadow);overflow:hidden;transition:box-shadow var(--transition)}.card:hover{box-shadow:var(--card-shadow-hover)}
.card-header{display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--border-light);background:var(--ms-pale);cursor:pointer;user-select:none}.card-header:hover{background:var(--ms-ice)}
.card-header h3{font-size:15px;font-weight:600;color:var(--ms-navy);display:flex;align-items:center;gap:10px}
.card-icon{width:30px;height:30px;border-radius:7px;display:flex;align-items:center;justify-content:center;font-size:15px;flex-shrink:0}
.icon-profile{background:#E8F0FE;color:var(--ms-accent)}.icon-account{background:#E6F4EA;color:var(--success)}.icon-contrib{background:#FFF3E0;color:var(--warning)}.icon-tax{background:#FCE4EC;color:var(--danger)}.icon-convert{background:#EDE7F6;color:#6A4FC8}.icon-brokerage{background:#E0F2F1;color:#00796B}
.chevron{width:20px;height:20px;color:var(--text-muted);transition:transform var(--transition)}.card.collapsed .chevron{transform:rotate(-90deg)}
.card-body{padding:20px 24px 24px;display:grid;gap:16px}.card.collapsed .card-body{display:none}
.field{display:flex;flex-direction:column;gap:5px}.field-row{display:grid;grid-template-columns:1fr 1fr;gap:16px}
@media(max-width:600px){.field-row{grid-template-columns:1fr}}
label{font-size:12.5px;font-weight:600;color:var(--text-secondary);letter-spacing:0.3px;display:flex;align-items:center;gap:4px}
label .req{color:var(--danger);font-size:11px}label .opt{color:var(--text-muted);font-weight:400;font-size:11px}
.hint{font-size:11.5px;color:var(--text-muted);margin-top:-2px}
.section-divider{border:none;border-top:1px dashed var(--border);margin:4px 0}
.section-label{font-size:12px;color:var(--ms-blue);font-weight:700;letter-spacing:0.5px;text-transform:uppercase}
input[type="number"],input[type="text"],select{width:100%;padding:10px 14px;font-family:'DM Sans',sans-serif;font-size:14px;color:var(--text-primary);background:var(--ms-white);border:1.5px solid var(--border);border-radius:var(--radius-sm);outline:none;transition:border-color var(--transition),box-shadow var(--transition)}
input:focus,select:focus{border-color:var(--ms-accent);box-shadow:0 0 0 3px rgba(0,120,200,0.1)}
input::placeholder{color:var(--text-muted);font-weight:300}input::-webkit-inner-spin-button{-webkit-appearance:none}input[type="number"]{-moz-appearance:textfield}select{cursor:pointer}
.input-wrap{position:relative;display:flex;align-items:center}.input-wrap .prefix,.input-wrap .suffix{position:absolute;font-size:13px;color:var(--text-muted);font-weight:500;pointer-events:none}.input-wrap .prefix{left:14px}.input-wrap .suffix{right:14px}.input-wrap input.has-prefix{padding-left:28px}.input-wrap input.has-suffix{padding-right:32px}
.check-row{display:flex;align-items:center;gap:8px;margin-top:2px}.check-row input[type="checkbox"]{width:16px;height:16px;accent-color:var(--ms-accent);cursor:pointer}.check-row label{font-weight:500;cursor:pointer;font-size:13px}
.disabled-section{opacity:0.4;pointer-events:none}
.range-group{display:flex;flex-direction:column;gap:6px}.range-header{display:flex;justify-content:space-between;align-items:center}
input[type="range"]{-webkit-appearance:none;width:100%;height:6px;border-radius:3px;background:linear-gradient(to right,var(--ms-accent) 0%,var(--ms-accent) var(--pct,50%),var(--border-light) var(--pct,50%));outline:none;border:none;cursor:pointer}
input[type="range"]::-webkit-slider-thumb{-webkit-appearance:none;width:20px;height:20px;border-radius:50%;background:white;border:3px solid var(--ms-accent);box-shadow:0 1px 4px rgba(0,0,0,0.15);cursor:pointer}
input[type="range"]::-moz-range-thumb{width:20px;height:20px;border-radius:50%;background:white;border:3px solid var(--ms-accent);box-shadow:0 1px 4px rgba(0,0,0,0.15);cursor:pointer}
.conversion-slider-group{background:linear-gradient(135deg,var(--ms-pale),white);border:1px solid var(--ms-ice);border-radius:var(--radius);padding:20px;text-align:center}
.conversion-slider-group .range-value{font-size:26px;font-weight:700;color:var(--ms-blue);background:none;padding:0}.conversion-amount{font-size:13px;color:var(--text-secondary);margin-top:2px}
.actions{display:flex;gap:14px;margin-bottom:36px;flex-wrap:wrap}
.btn{padding:13px 32px;font-family:'DM Sans',sans-serif;font-size:14px;font-weight:600;border:none;border-radius:var(--radius-sm);cursor:pointer;transition:all var(--transition);letter-spacing:0.3px}
.btn-primary{background:linear-gradient(135deg,var(--ms-blue),var(--ms-accent));color:white;box-shadow:0 2px 10px rgba(0,60,113,0.25)}.btn-primary:hover{box-shadow:0 4px 20px rgba(0,60,113,0.35);transform:translateY(-1px)}
.btn-secondary{background:white;color:var(--ms-blue);border:1.5px solid var(--border)}.btn-secondary:hover{border-color:var(--ms-accent);background:var(--ms-pale)}
.btn-print{background:var(--ms-pale);color:var(--ms-blue);margin-left:auto}.btn-print:hover{background:var(--ms-ice)}
.btn-sm{padding:6px 14px;font-size:12px;border-radius:4px}
.results{display:none}.results.visible{display:block}
.results-header{text-align:center;margin-bottom:32px;padding-bottom:24px;border-bottom:2px solid var(--ms-ice)}
.results-header h2{font-family:'Cormorant Garamond',serif;font-size:30px;font-weight:600;color:var(--ms-navy);margin-bottom:4px}.results-header p{color:var(--text-secondary);font-size:14px}
.summary-tiles{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:16px;margin-bottom:36px}
.final-outcomes{background:var(--ms-pale);border:1px solid var(--border-light);border-radius:var(--radius);padding:18px;margin:0 0 28px 0}
.final-outcomes h3{font-family:'Cormorant Garamond',serif;font-size:22px;margin-bottom:10px}
.final-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:14px}
.final-card{background:white;border:1px solid var(--border-light);border-radius:var(--radius);padding:14px}
.final-card h4{font-size:13px;letter-spacing:0.06em;text-transform:uppercase;color:var(--text-muted);margin-bottom:10px}
.final-row{display:flex;justify-content:space-between;gap:12px;margin:6px 0;font-size:14px}
.final-row .k{color:var(--text-secondary)}.final-row .v{font-weight:700}
.final-row.total{border-top:1px solid var(--border-light);padding-top:6px;margin-top:8px}
.tile{background:white;border:1px solid var(--border-light);border-radius:var(--radius);padding:20px;text-align:center;box-shadow:var(--card-shadow);position:relative;overflow:hidden}
.tile::before{content:'';position:absolute;top:0;left:0;right:0;height:3px}
.tile.tile-blue::before{background:var(--ms-accent)}.tile.tile-green::before{background:var(--success)}.tile.tile-gold::before{background:var(--ms-gold)}.tile.tile-red::before{background:var(--danger)}.tile.tile-purple::before{background:#6A4FC8}
.tile-label{font-size:11.5px;font-weight:600;color:var(--text-muted);text-transform:uppercase;letter-spacing:1px;margin-bottom:6px}
.tile-value{font-size:24px;font-weight:700;color:var(--ms-navy)}.tile-sub{font-size:12px;color:var(--text-muted);margin-top:4px}
.chart-section{background:white;border:1px solid var(--border-light);border-radius:var(--radius);box-shadow:var(--card-shadow);padding:28px;margin-bottom:24px}
.chart-section h3{font-family:'Cormorant Garamond',serif;font-size:22px;font-weight:600;color:var(--ms-navy);margin-bottom:4px}
.chart-desc{font-size:13px;color:var(--text-secondary);margin-bottom:20px;line-height:1.65}.chart-container{position:relative;width:100%;height:360px}
.chart-grid{display:grid;grid-template-columns:1fr 1fr;gap:24px}@media(max-width:900px){.chart-grid{grid-template-columns:1fr}}
.chart-grid .chart-container{height:300px}.chart-grid-label{text-align:center;font-size:13px;font-weight:600;color:var(--ms-blue);margin-bottom:8px}
.insight-box{background:linear-gradient(135deg,var(--ms-pale),white);border:1px solid var(--ms-ice);border-left:4px solid var(--ms-accent);border-radius:0 var(--radius) var(--radius) 0;padding:20px 24px;margin-top:20px}
.insight-box h4{font-size:13px;font-weight:700;color:var(--ms-blue);text-transform:uppercase;letter-spacing:1px;margin-bottom:6px}
.insight-box p{font-size:13.5px;color:var(--text-secondary);line-height:1.7}.hl{color:var(--ms-blue);font-weight:600}
.table-wrapper{overflow-x:auto;margin-top:20px;border-radius:var(--radius-sm);border:1px solid var(--border-light);max-height:500px;overflow-y:auto}
table{width:100%;border-collapse:collapse;font-size:12px}
thead th{background:var(--ms-navy);color:white;padding:10px 12px;text-align:right;font-weight:600;font-size:11px;letter-spacing:0.3px;white-space:nowrap;position:sticky;top:0;z-index:2}
thead th:first-child{text-align:left}thead th.gN{background:#1a3a50}thead th.gI{background:#1a4a3a}thead th.gO{background:#003060}
tbody td{padding:7px 12px;text-align:right;border-bottom:1px solid var(--border-light);white-space:nowrap;font-size:12px}
tbody td:first-child{text-align:left;font-weight:500;color:var(--ms-blue)}
tbody tr:nth-child(even){background:var(--ms-pale)}tbody tr:hover{background:var(--ms-ice)}
.table-note{font-size:11.5px;color:var(--text-muted);margin-top:8px;font-style:italic}
.recommendation{background:linear-gradient(135deg,var(--ms-navy),var(--ms-blue));color:white;border-radius:var(--radius);padding:32px;margin-bottom:24px;position:relative;overflow:hidden}
.recommendation::before{content:'';position:absolute;top:-50%;right:-15%;width:300px;height:300px;background:radial-gradient(circle,rgba(255,255,255,0.04),transparent);border-radius:50%}
.recommendation h3{font-family:'Cormorant Garamond',serif;font-size:24px;font-weight:600;margin-bottom:12px}
.recommendation p{font-size:14px;line-height:1.75;color:rgba(255,255,255,0.85)}
.rec-badge{display:inline-block;padding:4px 14px;border-radius:20px;font-size:12px;font-weight:700;text-transform:uppercase;letter-spacing:1px;margin-bottom:12px}
.rec-favorable{background:rgba(27,138,90,0.3);color:#7DEBB5}.rec-unfavorable{background:rgba(200,50,50,0.3);color:#FFB0B0}
.disclaimer{background:var(--ms-pale);border:1px solid var(--border-light);border-radius:var(--radius);padding:24px;margin-top:36px}
.disclaimer h4{font-size:12px;font-weight:700;color:var(--text-secondary);text-transform:uppercase;letter-spacing:1px;margin-bottom:8px}
.disclaimer p{font-size:12px;color:var(--text-muted);line-height:1.7}
.legend-box{display:flex;flex-wrap:wrap;gap:16px;margin-bottom:16px;padding:12px 16px;background:var(--ms-pale);border-radius:var(--radius-sm);font-size:12px}
.legend-item{display:flex;align-items:center;gap:6px}.legend-dot{width:12px;height:12px;border-radius:3px}
.yby-table{max-height:280px;overflow-y:auto;border:1px solid var(--border-light);border-radius:var(--radius-sm);margin-top:8px}
.yby-table table{font-size:12px}.yby-table th{position:sticky;top:0;z-index:1;background:var(--ms-navy);color:white;padding:6px 8px;font-size:11px}
.yby-table td{padding:4px 8px}.yby-table input{width:60px;padding:4px 6px;font-size:12px;text-align:center;border:1px solid var(--border);border-radius:3px}
@media print{body{font-size:11px}.header{-webkit-print-color-adjust:exact;print-color-adjust:exact}.actions,.card-header .chevron{display:none}.card.collapsed .card-body{display:grid}.chart-container{height:260px!important}.results{display:block!important}.main{padding:20px}}
@keyframes fadeUp{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:translateY(0)}}
.results.visible .summary-tiles,.results.visible .chart-section,.results.visible .recommendation{animation:fadeUp 0.4s ease-out both}
</style>
</head>
<body>
<header class="header"><div class="header-inner">
  <div class="header-brand"><div class="header-logo">V</div>
    <div class="header-titles"><h2>The Vinder Group &nbsp;|&nbsp; Morgan Stanley</h2><h1>Retirement Account Value Simulator</h1></div>
  </div><p class="header-subtitle">Roth Conversion Calculator</p>
</div></header>
<div class="main">
<div class="input-grid">
  <div class="card" id="c1"><div class="card-header" onclick="toggleCard('c1')"><h3><span class="card-icon icon-profile">👤</span> Client Profile</h3><svg class="chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg></div>
  <div class="card-body">
    <div class="field-row"><div class="field"><label>Current Age <span class="req">*</span></label><input type="number" id="age" placeholder="e.g. 52" min="18" max="100"></div><div class="field"><label>Retirement Age</label><input type="number" id="retireAge" placeholder="65" min="50" max="100" value="65"></div></div>
    <div class="field-row"><div class="field"><label>Life Expectancy</label><input type="number" id="lifeExpect" placeholder="90" min="60" max="110" value="90"></div><div class="field"></div></div>
  </div></div>
  <div class="card" id="c2"><div class="card-header" onclick="toggleCard('c2')"><h3><span class="card-icon icon-account">💰</span> Account Information</h3><svg class="chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg></div>
  <div class="card-body">
    <div class="field"><label>Traditional IRA / Pre-Tax Balance <span class="req">*</span></label><div class="input-wrap"><span class="prefix">$</span><input type="number" id="tradBalance" class="has-prefix" placeholder="e.g. 500,000" min="0"></div></div>
    <div class="field"><label>Existing Roth IRA Balance <span class="opt">(optional)</span></label><div class="input-wrap"><span class="prefix">$</span><input type="number" id="rothBalance" class="has-prefix" placeholder="0" min="0" value="0"></div></div>
    <div class="field"><label>Account Type</label><select id="accountType"><option value="trad_ira" selected>Traditional IRA</option><option value="sep_ira">SEP IRA</option><option value="simple_ira">SIMPLE IRA</option><option value="401k">401(k) / 403(b)</option></select><span class="hint">For labeling purposes only — does not affect calculations</span></div>
  </div></div>
  <div class="card" id="c3"><div class="card-header" onclick="toggleCard('c3')"><h3><span class="card-icon icon-contrib">📈</span> Contributions &amp; Growth</h3><svg class="chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg></div>
  <div class="card-body">
    <div class="check-row"><input type="checkbox" id="maxContrib" onchange="toggleMaxContrib()"><label for="maxContrib">Contribute IRA maximum ($7,500 under 50 · $8,600 age 50+)</label></div>
    <div id="contribManual"><div class="field-row"><div class="field"><label>Annual Contributions <span class="opt">(optional)</span></label><div class="input-wrap"><span class="prefix">$</span><input type="number" id="annualContrib" class="has-prefix" placeholder="0" min="0" value="0"></div></div><div class="field"><label>Contribution Frequency</label><select id="contribFreq"><option value="annual" selected>Annual</option><option value="monthly">Monthly</option></select></div></div></div>
    <div class="check-row"><input type="checkbox" id="contribLifelong" checked><label for="contribLifelong">Continue contributions through life expectancy</label></div>
    <span class="hint">When unchecked, contributions stop at retirement age. Requires earned income in practice.</span>
    <div class="field range-group"><div class="range-header"><label>Expected IRA Return</label><div style="display:flex;align-items:center;gap:8px"><input type="number" id="annualReturnNum" min="1" max="14" step="0.01" value="6.50" style="width:72px;padding:5px 8px;font-size:13px;text-align:center;border-radius:4px" oninput="syncSliderFromInput()"><span style="font-size:13px;color:var(--text-muted)">%</span></div></div><input type="range" id="annualReturn" min="1" max="14" step="0.01" value="6.5" oninput="syncInputFromSlider()"><span class="hint">Growth rate for both Traditional IRA and Roth IRA</span></div>
  </div></div>
  <div class="card" id="c4"><div class="card-header" onclick="toggleCard('c4')"><h3><span class="card-icon icon-tax">📋</span> Tax Information</h3><svg class="chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg></div>
  <div class="card-body">
    <label class="section-label">Pre-Retirement Income Tax</label>
    <div class="field-row"><div class="field"><label>Federal Rate <span class="req">*</span></label><div class="input-wrap"><input type="number" id="currentTaxRate" class="has-suffix" placeholder="e.g. 36" min="0" max="50" step="0.1"><span class="suffix">%</span></div></div><div class="field"><label>State Rate <span class="opt">(optional)</span></label><div class="input-wrap"><input type="number" id="stateTaxRate" class="has-suffix" placeholder="0" min="0" max="15" step="0.1" value="0"><span class="suffix">%</span></div></div></div>
    <hr class="section-divider">
    <label class="section-label">Post-Retirement Income Tax</label>
    <div class="field-row"><div class="field"><label>Federal Rate</label><div class="input-wrap"><input type="number" id="retireTaxRate" class="has-suffix" placeholder="e.g. 24" min="0" max="50" step="0.1" value="24"><span class="suffix">%</span></div></div><div class="field"><label>State Rate <span class="opt">(optional)</span></label><div class="input-wrap"><input type="number" id="retireStateTaxRate" class="has-suffix" placeholder="0" min="0" max="15" step="0.1" value="0"><span class="suffix">%</span></div></div></div>
    <hr class="section-divider">
    <label class="section-label">Capital Gains Tax (Brokerage Gains)</label>
    <div class="field-row"><div class="field"><label>Federal CG Rate</label><div class="input-wrap"><input type="number" id="cgFed" class="has-suffix" placeholder="15" min="0" max="30" step="0.1" value="15"><span class="suffix">%</span></div></div><div class="field"><label>State CG Rate <span class="opt">(optional)</span></label><div class="input-wrap"><input type="number" id="cgState" class="has-suffix" placeholder="0" min="0" max="15" step="0.1" value="0"><span class="suffix">%</span></div></div></div>
    <hr class="section-divider">
    <div class="check-row"><input type="checkbox" id="ybyEnabled" onchange="toggleYBY()"><label for="ybyEnabled">Override tax rates year-by-year <span class="opt">(advanced)</span></label></div>
    <div id="ybySection" class="disabled-section">
      <span class="hint">Customize federal + state income tax for each year. Pre-populated from rates above.</span>
      <div style="margin:8px 0"><button class="btn btn-sm btn-secondary" onclick="generateYBY()">Generate / Reset Table</button></div>
      <div id="ybyTableWrap" class="yby-table"></div>
    </div>
  </div></div>
  <div class="card" id="c5"><div class="card-header" onclick="toggleCard('c5')"><h3><span class="card-icon icon-brokerage">🏦</span> RMD Brokerage Reinvestment <span class="opt" style="margin-left:4px">(optional)</span></h3><svg class="chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg></div>
  <div class="card-body">
    <div class="check-row"><input type="checkbox" id="brokEnabled" checked onchange="toggleBrok()"><label for="brokEnabled">Reinvest RMD proceeds in taxable brokerage</label></div>
    <div id="brokFields">
      <div class="field-row" style="margin-top:12px">
        <div class="field"><label>% of After-Tax RMD to Invest</label><div class="input-wrap"><input type="number" id="brokInvestPct" class="has-suffix" placeholder="100" min="0" max="100" step="1" value="100"><span class="suffix">%</span></div><span class="hint">Remainder assumed spent</span></div>
        <div class="field"><label>Brokerage Return Rate</label><div class="input-wrap"><input type="number" id="brokReturn" class="has-suffix" placeholder="6" min="0" max="20" step="0.01" value="6"><span class="suffix">%</span></div></div>
      </div>
    </div>
  </div></div>
  <div class="card" id="c6" style="grid-column:1/-1"><div class="card-header" onclick="toggleCard('c6')"><h3><span class="card-icon icon-convert">🔄</span> Conversion Strategy</h3><svg class="chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg></div>
  <div class="card-body">
    <div class="conversion-slider-group"><label style="justify-content:center;margin-bottom:8px">Conversion Percentage</label><div class="range-value" id="convPctDisplay">100%</div><div class="conversion-amount" id="convAmtDisplay">of $0</div><input type="range" id="conversionPct" min="0" max="100" step="5" value="100" oninput="updateConvSlider()" style="margin-top:12px"></div>
    <div class="field"><label>Multi-Year Conversion</label><select id="multiYear"><option value="1" selected>Convert all in Year 1</option><option value="2">Spread over 2 years</option><option value="3">Spread over 3 years</option><option value="4">Spread over 4 years</option><option value="5">Spread over 5 years</option></select></div>
    <hr class="section-divider">
    <label class="section-label">Scenario Display Options</label>
    <div class="check-row"><input type="checkbox" id="hideIRA" checked><label for="hideIRA">Do not display "Pay from IRA" data</label></div>
    <div class="check-row" style="margin-top:6px"><input type="checkbox" id="outsideEnabled" checked onchange="toggleOutside()"><label for="outsideEnabled">Include "Pay from Outside Account" scenario</label></div>
    <div id="outsideFields">
      <div class="field" style="margin-top:12px"><label>Outside Brokerage / Portfolio Return</label><div class="input-wrap"><input type="number" id="outsideReturn" class="has-suffix" placeholder="8" min="0" max="20" step="0.01" value="8"><span class="suffix">%</span></div><span class="hint">Return on the outside portfolio. In No Conversion &amp; Pay from IRA, this money stays invested. In Pay from Outside, it's used to pay tax.</span></div>
    </div>
  </div></div>
</div>
<div class="actions">
  <button class="btn btn-primary" onclick="runAnalysis()">Run Conversion Analysis</button>
  <button class="btn btn-secondary" onclick="resetForm()">Reset All</button>
  <button class="btn btn-print" onclick="window.print()">🖨 Print Report</button>
</div>
<div class="results" id="results">
  <div class="results-header"><h2>Roth Conversion Analysis Results</h2><p>Prepared on <span id="analysisDate"></span></p></div>
  <div class="legend-box" id="legendBox"></div>
  <div class="summary-tiles" id="summaryTiles"></div>
  <div class="recommendation" id="recBox"></div>
  <div class="final-outcomes" id="finalOutcomes"></div>
  <div class="chart-section"><h3>After-Tax Wealth — All Scenarios</h3>
    <p class="chart-desc" id="descWealth"></p>
    <div class="chart-container"><canvas id="chartWealth"></canvas></div><div class="insight-box" id="insightWealth"></div></div>
  <div class="chart-section"><h3>Tax Impact Comparison</h3>
    <p class="chart-desc">Cumulative and annual taxes across scenarios.</p>
    <div class="chart-grid"><div><div class="chart-grid-label">Cumulative Taxes Paid</div><div class="chart-container"><canvas id="chartTaxCumul"></canvas></div></div><div><div class="chart-grid-label">Annual Tax Burden</div><div class="chart-container"><canvas id="chartTaxAnnual"></canvas></div></div></div>
    <div class="insight-box" id="insightTax"></div></div>
  <div class="chart-section"><h3>Required Minimum Distributions (RMDs)</h3>
    <p class="chart-desc">Starting age 75 per SECURE 2.0. Full conversion eliminates all RMDs.</p>
    <div class="chart-container"><canvas id="chartRMD"></canvas></div><div class="insight-box" id="insightRMD"></div></div>
  <div class="chart-section" id="sectionMultiYear" style="display:none"><h3>Multi-Year Conversion Strategy</h3>
    <div class="chart-container"><canvas id="chartMultiYear"></canvas></div><div class="insight-box" id="insightMultiYear"></div></div>
  <div class="chart-section"><h3>Year-by-Year Projection</h3>
    <p class="chart-desc">🟢 = Pay-from-IRA crossover. 🔵 = Pay-from-Outside crossover. ⭐ = Retirement age. 📋 = RMDs begin (age 75).</p>
    <div class="table-wrapper" id="projTable"></div>
    <p class="table-note" id="tableNote"></p></div>
  <div class="disclaimer"><h4>Important Disclosures</h4>
    <p>We are presenting the information contained herein pursuant to our customer's request. It is valid as of the date of issuance and is subject to change. Morgan Stanley does not warranty or guaranty that such identified securities, assets or monies will remain in the customer's account. The customer has the full power to withdraw assets from this account at any time and no security interest or collateral rights are being granted to any party other than Morgan Stanley to the extent of any debit in the account.</p>
    <p style="margin-top:12px">The information and data contained in this report are from sources considered reliable, but their accuracy and completeness is not guaranteed. This report has been prepared for illustrative purposes only and is not intended to be used as a substitute for monthly transaction statements you receive on a regular basis from Morgan Stanley Smith Barney LLC. Please compare the data on this document carefully with your monthly statements to verify its accuracy. The Company strongly encourages you to consult with your own accountants or other advisors with respect to any tax questions.</p>
    <p style="margin-top:12px"><strong>Methodology:</strong> Scenarios compared using flat tax rates (or year-by-year overrides). "Pay from IRA" deducts tax from the converted amount. "Pay from Outside" preserves the full Roth balance; the tax cost is removed from an outside brokerage. In No Conversion and Pay from IRA, that outside brokerage remains and compounds — providing a true apples-to-apples comparison.</p></div>
</div>
</div>
<script>
function $(id){return document.getElementById(id)}
function val(id,fb){var v=parseFloat($(id).value);return isNaN(v)?fb:v}
function fmt(n){if(Math.abs(n)>=1e6)return'$'+(n/1e6).toFixed(2)+'M';return'$'+Math.round(n).toLocaleString()}
function fK(n){return'$'+Math.round(n).toLocaleString()}
function pct(n){return n.toFixed(1)+'%'}
function toggleCard(id){$(id).classList.toggle('collapsed')}
function syncInputFromSlider(){var v=parseFloat($('annualReturn').value);$('annualReturnNum').value=v.toFixed(2);$('annualReturn').style.setProperty('--pct',(($('annualReturn').value-1)/13)*100+'%')}
function syncSliderFromInput(){var v=parseFloat($('annualReturnNum').value);if(!isNaN(v)&&v>=1&&v<=14){$('annualReturn').value=v;$('annualReturn').style.setProperty('--pct',((v-1)/13)*100+'%')}}
function updateConvSlider(){var p=val('conversionPct',100),b=val('tradBalance',0);$('convPctDisplay').textContent=p+'%';$('convAmtDisplay').textContent='Converting '+fK(b*p/100)+' of '+fK(b);$('conversionPct').style.setProperty('--pct',p+'%')}
function toggleMaxContrib(){$('contribManual').style.display=$('maxContrib').checked?'none':'block'}
function toggleBrok(){$('brokFields').className=$('brokEnabled').checked?'':'disabled-section'}
function toggleOutside(){$('outsideFields').className=$('outsideEnabled').checked?'':'disabled-section'}
function toggleYBY(){$('ybySection').className=$('ybyEnabled').checked?'':'disabled-section'}
function generateYBY(){
  var a=val('age',50),le=val('lifeExpect',90),ra=val('retireAge',65);
  var fPre=val('currentTaxRate',36),sPre=val('stateTaxRate',0),fPost=val('retireTaxRate',24),sPost=val('retireStateTaxRate',0);
  var h='<table><thead><tr><th>Age</th><th>Fed %</th><th>State %</th><th>Total</th></tr></thead><tbody>';
  for(var i=a;i<=le;i++){var f=i<ra?fPre:fPost,s=i<ra?sPre:sPost;h+='<tr><td style="font-weight:600">'+i+(i==ra?' ⭐':'')+'</td><td><input type="number" id="yf_'+i+'" value="'+f.toFixed(1)+'" step="0.1" min="0" max="50"></td><td><input type="number" id="ys_'+i+'" value="'+s.toFixed(1)+'" step="0.1" min="0" max="15"></td><td id="yt_'+i+'" style="font-weight:600">'+(f+s).toFixed(1)+'%</td></tr>'}
  h+='</tbody></table>';$('ybyTableWrap').innerHTML=h;
  for(var j=a;j<=le;j++){(function(age){var fE=$('yf_'+age),sE=$('ys_'+age);function u(){$('yt_'+age).textContent=((parseFloat(fE.value)||0)+(parseFloat(sE.value)||0)).toFixed(1)+'%'}fE.oninput=u;sE.oninput=u})(j)}
}
document.addEventListener('DOMContentLoaded',function(){
  document.querySelectorAll('input[type="range"]').forEach(function(el){el.style.setProperty('--pct',((el.value-el.min)/(el.max-el.min))*100+'%')});
  $('tradBalance').addEventListener('input',updateConvSlider);syncInputFromSlider();
});
function resetForm(){
  document.querySelectorAll('input[type="number"]').forEach(function(i){i.value=i.defaultValue||''});
  document.querySelectorAll('select').forEach(function(s){for(var j=0;j<s.options.length;j++)if(s.options[j].defaultSelected){s.selectedIndex=j;break}});
  $('maxContrib').checked=false;toggleMaxContrib();$('brokEnabled').checked=true;toggleBrok();
  $('outsideEnabled').checked=true;toggleOutside();$('hideIRA').checked=true;$('ybyEnabled').checked=false;toggleYBY();$('contribLifelong').checked=true;
  $('annualReturn').value=6.5;$('annualReturnNum').value='6.50';syncInputFromSlider();
  $('conversionPct').value=100;updateConvSlider();$('results').classList.remove('visible');charts.forEach(function(c){if(c)c.destroy()});charts=[];
}
var RMDT={75:24.6,76:23.7,77:22.9,78:22.0,79:21.1,80:20.2,81:19.4,82:18.5,83:17.7,84:16.8,85:16.0,86:15.2,87:14.4,88:13.7,89:12.9,90:12.2,91:11.5,92:10.8,93:10.1,94:9.5,95:8.9,96:8.4,97:7.8,98:7.3,99:6.8,100:6.4,101:6.0,102:5.6,103:5.2,104:4.9,105:4.6,106:4.3,107:4.1,108:3.9,109:3.7,110:3.5};
function rmdF(a){return RMDT[a]||Math.max(3.5,24.6-(a-75)*0.45)}
function maxCA(age){return age<50?7500:8600}
var charts=[];
var C_NO='#7A8A9A',C_IP='#1B8A5A',C_OP='#0078C8';

function runAnalysis(){
  var age=val('age',null),tradBal=val('tradBalance',null),curTax=val('currentTaxRate',null);
  if(age===null||tradBal===null||curTax===null){alert('Please fill required fields.');return}
  charts.forEach(function(c){if(c)c.destroy()});charts=[];
  var retAge=val('retireAge',65),lifeExp=val('lifeExpect',90);
  var rothBal=val('rothBalance',0),useMax=$('maxContrib').checked;
  var manC=val('annualContrib',0),cFq=$('contribFreq').value;
  var retRate=val('annualReturn',6.5)/100;
  var curTaxR=curTax/100,retTaxR=val('retireTaxRate',24)/100;
  var stPre=val('stateTaxRate',0)/100,stPost=val('retireStateTaxRate',0)/100;
  var multiYr=parseInt($('multiYear').value),convPct=val('conversionPct',100)/100;
  var outOn=$('outsideEnabled').checked,showIP=!$('hideIRA').checked;
  var outRetRate=val('outsideReturn',8)/100;
  var brokOn=$('brokEnabled').checked,bInvPct=brokOn?val('brokInvestPct',100)/100:0;
  var bRet=val('brokReturn',6)/100,cgRate=(val('cgFed',15)+val('cgState',0))/100;
  var contribLife=$('contribLifelong').checked;
  var totalYrs=lifeExp-age,convAmt=tradBal*convPct;
  var preTaxR=curTaxR+stPre,postTaxR=retTaxR+stPost;
  var useYBY=$('ybyEnabled').checked&&$('yf_'+age);
  var startYear=new Date().getFullYear();

  function iraTR(ca){if(useYBY){var f=$('yf_'+ca),s=$('ys_'+ca);if(f&&s)return(parseFloat(f.value)||0)/100+(parseFloat(s.value)||0)/100}return ca<retAge?preTaxR:postTaxR}
  var taxCost=convAmt*preTaxR,taxPerYr=taxCost/multiYr;

  // STATE
  var noTrad=tradBal,noRoth=rothBal,noBrok=0,noBrokB=0,noOutB=0,noOutBB=0;
  if(outOn){noOutB=taxCost;noOutBB=taxCost}
  var ipRoth=rothBal+(convAmt-taxCost),ipTrad=tradBal-convAmt,ipBrok=0,ipBrokB=0,ipOutB=0,ipOutBB=0;
  if(outOn){ipOutB=taxCost;ipOutBB=taxCost}
  var opRoth=rothBal+convAmt,opTrad=tradBal-convAmt,opBrok=0,opBrokB=0;

  var D={labels:[],years:[],noTrad:[],noBrok:[],noBrokB:[],noOutB:[],noRMD:[],noW:[],ipRoth:[],ipTrad:[],ipBrok:[],ipBrokB:[],ipOutB:[],ipRMD:[],ipW:[],opRoth:[],opTrad:[],opBrok:[],opBrokB:[],opRMD:[],opW:[],noTaxY:[],ipTaxY:[],opTaxY:[],cumTNo:[],cumTIp:[],cumTOp:[]};
  var cumTNo=0,cumTIp=taxCost,cumTOp=taxCost,ipXAge=null,opXAge=null;
  var CROSS_THRESH=1000; // $1000 threshold to avoid rounding artifacts

  for(var y=0;y<=totalYrs;y++){
    var ca=age+y;D.labels.push(ca);D.years.push(startYear+y);
    var isRet=ca>=retAge,rmdReq=ca>=75;
    var yC=0;var canContrib=contribLife||!isRet;if(canContrib){yC=useMax?maxCA(ca):(cFq==='monthly'?manC*12:manC)}
    if(y>0){
      noTrad*=(1+retRate);noRoth*=(1+retRate);if(brokOn)noBrok*=(1+bRet);if(outOn)noOutB*=(1+outRetRate);
      ipRoth*=(1+retRate);ipTrad*=(1+retRate);if(brokOn)ipBrok*=(1+bRet);if(outOn)ipOutB*=(1+outRetRate);
      if(outOn){opRoth*=(1+retRate);opTrad*=(1+retRate);if(brokOn)opBrok*=(1+bRet)}
      if(yC>0){noTrad+=yC;var cR=yC*convPct,cT=yC*(1-convPct);ipRoth+=cR;ipTrad+=cT;if(outOn){opRoth+=cR;opTrad+=cT}}
    }
    var tr=iraTR(ca),rN=0,rI=0,rO=0;
    if(rmdReq){var f=rmdF(ca);
      if(noTrad>0){rN=noTrad/f;noTrad-=rN;if(brokOn){var a1=rN*(1-tr)*bInvPct;noBrok+=a1;noBrokB+=a1}}
      if(ipTrad>0){rI=ipTrad/f;ipTrad-=rI;if(brokOn){var a2=rI*(1-tr)*bInvPct;ipBrok+=a2;ipBrokB+=a2}}
      if(outOn&&opTrad>0){rO=opTrad/f;opTrad-=rO;if(brokOn){var a3=rO*(1-tr)*bInvPct;opBrok+=a3;opBrokB+=a3}}
    }
    var tN=0,tI=0,tO=0;
    if(isRet){tN=rN*tr;tI=rI*tr;tO=rO*tr;cumTNo+=tN;cumTIp+=tI;cumTOp+=tO}
    var cTY=(y<multiYr)?taxPerYr:0;
    var nBA=noBrok-Math.max(0,noBrok-noBrokB)*cgRate,nOA=noOutB-Math.max(0,noOutB-noOutBB)*cgRate;
    var iBA=ipBrok-Math.max(0,ipBrok-ipBrokB)*cgRate,iOA=ipOutB-Math.max(0,ipOutB-ipOutBB)*cgRate;
    var oBA=outOn?(opBrok-Math.max(0,opBrok-opBrokB)*cgRate):0;
    var wN=noRoth+noTrad*(1-tr)+nBA+nOA;
    var wI=ipRoth+ipTrad*(1-tr)+iBA+iOA;
    var wO=outOn?(opRoth+opTrad*(1-tr)+oBA):0;
    D.noTrad.push(Math.round(noTrad));D.noBrok.push(Math.round(noBrok));D.noBrokB.push(Math.round(noBrokB));D.noOutB.push(Math.round(noOutB));D.noRMD.push(Math.round(rN));D.noW.push(Math.round(wN));
    D.ipRoth.push(Math.round(ipRoth));D.ipTrad.push(Math.round(ipTrad));D.ipBrok.push(Math.round(ipBrok));D.ipBrokB.push(Math.round(ipBrokB));D.ipOutB.push(Math.round(ipOutB));D.ipRMD.push(Math.round(rI));D.ipW.push(Math.round(wI));
    D.opRoth.push(Math.round(outOn?opRoth:0));D.opTrad.push(Math.round(outOn?opTrad:0));D.opBrok.push(Math.round(outOn?opBrok:0));D.opBrokB.push(Math.round(outOn?opBrokB:0));D.opRMD.push(Math.round(rO));D.opW.push(Math.round(wO));
    D.noTaxY.push(Math.round(tN));D.ipTaxY.push(Math.round(tI+cTY));D.opTaxY.push(Math.round(tO+cTY));
    D.cumTNo.push(Math.round(cumTNo));D.cumTIp.push(Math.round(cumTIp));D.cumTOp.push(Math.round(cumTOp));
    if(ipXAge===null&&(wI-wN)>CROSS_THRESH&&y>0)ipXAge=ca;
    if(outOn&&opXAge===null&&(wO-wN)>CROSS_THRESH&&y>0)opXAge=ca;
  }
  var fi=D.labels.length-1;
  var nbI=D.ipW[fi]-D.noW[fi],nbO=outOn?(D.opW[fi]-D.noW[fi]):0;
  var totRMD=0;for(var r=0;r<D.noRMD.length;r++)totRMD+=D.noRMD[r];
  var pkRMD=0,pkAge=75;for(var pk=0;pk<D.noRMD.length;pk++){if(D.noRMD[pk]>pkRMD){pkRMD=D.noRMD[pk];pkAge=D.labels[pk]}}
  var best='no',bestW=D.noW[fi];
  if(showIP&&D.ipW[fi]>bestW){best='ip';bestW=D.ipW[fi]}
  if(outOn&&D.opW[fi]>bestW){best='op';bestW=D.opW[fi]}
  if(!showIP&&!outOn){best='no';bestW=D.noW[fi]} // edge case

  $('results').classList.add('visible');
  $('analysisDate').textContent=new Date().toLocaleDateString('en-US',{year:'numeric',month:'long',day:'numeric'});

  // LEGEND
  var lg='<div class="legend-item"><div class="legend-dot" style="background:'+C_NO+'"></div>No Conversion</div>';
  if(showIP)lg+='<div class="legend-item"><div class="legend-dot" style="background:'+C_IP+'"></div>Convert — Pay from IRA</div>';
  if(outOn)lg+='<div class="legend-item"><div class="legend-dot" style="background:'+C_OP+'"></div>Convert — Pay from Outside</div>';
  lg+='<div class="legend-item">⭐ Retirement</div><div class="legend-item">📋 RMDs Begin</div>';
  if(showIP)lg+='<div class="legend-item">🟢 IRA Crossover</div>';
  if(outOn)lg+='<div class="legend-item">🔵 Outside Crossover</div>';
  $('legendBox').innerHTML=lg;

  // TILES
  var tl='<div class="tile tile-red"><div class="tile-label">Upfront Tax Cost</div><div class="tile-value">'+fmt(taxCost)+'</div><div class="tile-sub">'+pct(preTaxR*100)+' on '+fmt(convAmt)+'</div></div>';
  if(showIP)tl+='<div class="tile tile-green"><div class="tile-label">Pay from IRA — Net</div><div class="tile-value" style="color:'+(nbI>=0?'var(--success)':'var(--danger)')+'">'+(nbI>=0?'+':'')+fmt(nbI)+'</div><div class="tile-sub">'+(ipXAge?'Crosses at age '+ipXAge:'No crossover')+'</div></div>';
  if(outOn)tl+='<div class="tile tile-blue"><div class="tile-label">Pay from Outside — Net</div><div class="tile-value" style="color:'+(nbO>=0?'var(--success)':'var(--danger)')+'">'+(nbO>=0?'+':'')+fmt(nbO)+'</div><div class="tile-sub">'+(opXAge?'Crosses at age '+opXAge:'No crossover')+'</div></div>';
  tl+='<div class="tile tile-gold"><div class="tile-label">Best Scenario</div><div class="tile-value" style="font-size:18px">'+(best==='no'?'No Conversion':best==='ip'?'Pay from IRA':'Pay from Outside')+'</div><div class="tile-sub">'+fmt(bestW)+' at age '+lifeExp+'</div></div>';
  $('summaryTiles').innerHTML=tl;

  // RECOMMENDATION
  var rc,rl,rt;
  if(best==='ip'){rc='rec-favorable';rl='Convert — Pay from IRA';rt='Paying tax from the IRA produces the best outcome: <strong>'+fmt(D.ipW[fi])+'</strong> by age '+lifeExp+' (<strong>'+fmt(nbI)+'</strong> over no conversion).'+(ipXAge?' Crosses at age '+ipXAge+'.':'');}
  else if(best==='op'){rc='rec-favorable';rl='Convert — Pay from Outside';rt='Paying from outside is optimal: <strong>'+fmt(D.opW[fi])+'</strong> by age '+lifeExp+' (<strong>'+fmt(nbO)+'</strong> over no conversion).'+(opXAge?' Crosses at age '+opXAge+'.':'');}
  else{rc='rec-unfavorable';rl='No Conversion Preferred';rt='Under these assumptions, no conversion is projected best: <strong>'+fmt(D.noW[fi])+'</strong>.';}
  $('recBox').innerHTML='<span class="rec-badge '+rc+'">'+rl+'</span><h3>Recommendation</h3><p>'+rt+'</p>';

  // FINAL OUTCOMES
  var trEnd=iraTR(lifeExp);
  var foH='<h3>Final Outcomes at Age '+lifeExp+'</h3><div class="final-grid">';
  foH+='<div class="final-card"><h4>No Conversion</h4><div class="final-row"><span class="k">Trad. IRA (gross)</span><span class="v">'+fmt(D.noTrad[fi])+'</span></div><div class="final-row"><span class="k">Trad. IRA (after tax)</span><span class="v">'+fmt(Math.round(D.noTrad[fi]*(1-trEnd)))+'</span></div>'+(D.noBrok[fi]>100?'<div class="final-row"><span class="k">RMD Brokerage (after CG)</span><span class="v">'+fmt(Math.round(D.noBrok[fi]-Math.max(0,D.noBrok[fi]-D.noBrokB[fi])*cgRate))+'</span></div>':'')+(outOn&&D.noOutB[fi]>100?'<div class="final-row"><span class="k">Outside Brokerage (after CG)</span><span class="v">'+fmt(Math.round(D.noOutB[fi]-Math.max(0,D.noOutB[fi]-noOutBB)*cgRate))+'</span></div>':'')+'<div class="final-row total"><span class="k">Total</span><span class="v">'+fmt(D.noW[fi])+'</span></div></div>';
  if(showIP){foH+='<div class="final-card"><h4>Pay from IRA</h4><div class="final-row"><span class="k">Roth</span><span class="v">'+fmt(D.ipRoth[fi])+'</span></div>'+(D.ipTrad[fi]>100?'<div class="final-row"><span class="k">Rem. Trad. (after tax)</span><span class="v">'+fmt(Math.round(D.ipTrad[fi]*(1-trEnd)))+'</span></div>':'')+(D.ipBrok[fi]>100?'<div class="final-row"><span class="k">RMD Brokerage (after CG)</span><span class="v">'+fmt(Math.round(D.ipBrok[fi]-Math.max(0,D.ipBrok[fi]-D.ipBrokB[fi])*cgRate))+'</span></div>':'')+(outOn&&D.ipOutB[fi]>100?'<div class="final-row"><span class="k">Outside Brokerage (after CG)</span><span class="v">'+fmt(Math.round(D.ipOutB[fi]-Math.max(0,D.ipOutB[fi]-ipOutBB)*cgRate))+'</span></div>':'')+'<div class="final-row total"><span class="k">Total</span><span class="v">'+fmt(D.ipW[fi])+'</span></div><div class="final-row"><span class="k">vs. No Conv.</span><span class="v" style="color:'+(nbI>=0?'var(--success)':'var(--danger)')+'">'+(nbI>=0?'+':'')+fmt(nbI)+'</span></div></div>'}
  if(outOn){foH+='<div class="final-card"><h4>Pay from Outside</h4><div class="final-row"><span class="k">Roth</span><span class="v">'+fmt(D.opRoth[fi])+'</span></div>'+(D.opTrad[fi]>100?'<div class="final-row"><span class="k">Rem. Trad. (after tax)</span><span class="v">'+fmt(Math.round(D.opTrad[fi]*(1-trEnd)))+'</span></div>':'')+(D.opBrok[fi]>100?'<div class="final-row"><span class="k">RMD Brokerage (after CG)</span><span class="v">'+fmt(Math.round(D.opBrok[fi]-Math.max(0,D.opBrok[fi]-D.opBrokB[fi])*cgRate))+'</span></div>':'')+'<div class="final-row"><span class="k">Outside Brokerage</span><span class="v" style="color:var(--text-muted)">$0 (used for tax)</span></div><div class="final-row total"><span class="k">Total</span><span class="v">'+fmt(D.opW[fi])+'</span></div><div class="final-row"><span class="k">vs. No Conv.</span><span class="v" style="color:'+(nbO>=0?'var(--success)':'var(--danger)')+'">'+(nbO>=0?'+':'')+fmt(nbO)+'</span></div></div>'}
  foH+='</div>';$('finalOutcomes').innerHTML=foH;

  // CHARTS
  var cDf={responsive:true,maintainAspectRatio:false,plugins:{legend:{position:'top',labels:{font:{family:"'DM Sans'",size:12},usePointStyle:true,pointStyleWidth:16,padding:16}},tooltip:{backgroundColor:'rgba(0,38,58,0.92)',titleFont:{family:"'DM Sans'",size:13},bodyFont:{family:"'DM Sans'",size:12},padding:12,cornerRadius:8,callbacks:{label:function(c){return c.dataset.label+': '+fK(c.raw)}}}},scales:{x:{title:{display:true,text:'Age',font:{family:"'DM Sans'",size:12,weight:'600'},color:'#4A5A6A'},ticks:{font:{family:"'DM Sans'",size:11},color:'#7A8A9A',maxTicksLimit:15},grid:{display:false}},y:{title:{display:true,text:'Value ($)',font:{family:"'DM Sans'",size:12,weight:'600'},color:'#4A5A6A'},ticks:{font:{family:"'DM Sans'",size:11},color:'#7A8A9A',callback:function(v){return v>=1e6?(v/1e6).toFixed(1)+'M':v>=1e3?(v/1e3).toFixed(0)+'K':v}},grid:{color:'#E8EEF2'}}}};
  function cd(){return JSON.parse(JSON.stringify(cDf))}

  var wDS=[{label:'No Conversion',data:D.noW,borderColor:C_NO,borderWidth:2,borderDash:[6,4],fill:false,tension:0.3,pointRadius:0}];
  if(showIP)wDS.push({label:'Pay from IRA',data:D.ipW,borderColor:C_IP,borderWidth:2.5,fill:false,tension:0.3,pointRadius:0});
  if(outOn)wDS.push({label:'Pay from Outside',data:D.opW,borderColor:C_OP,borderWidth:2.5,fill:false,tension:0.3,pointRadius:0});
  charts.push(new Chart($('chartWealth'),{type:'line',data:{labels:D.labels,datasets:wDS},options:cd()}));
  var dW='What you <strong>actually own</strong> at each age — all accounts after tax.';
  if(outOn)dW+=' No Conversion'+(showIP?' and Pay from IRA':'')+' include the outside brokerage ('+fmt(taxCost)+' growing at '+pct(outRetRate*100)+'). Pay from Outside does not — those funds paid the tax.';
  dW+=' All brokerage values net of capital gains tax on growth.';
  $('descWealth').innerHTML=dW;
  var iW='No Conv: <span class="hl">'+fmt(D.noW[fi])+'</span>.';
  if(showIP)iW+=' Pay from IRA: <span class="hl">'+fmt(D.ipW[fi])+'</span> (Roth '+fmt(D.ipRoth[fi])+').';
  if(outOn)iW+=' Pay from Outside: <span class="hl">'+fmt(D.opW[fi])+'</span> (Roth '+fmt(D.opRoth[fi])+').';
  $('insightWealth').innerHTML='<h4>Wealth at Age '+lifeExp+'</h4><p>'+iW+'</p>';

  // Tax charts
  var tcO=cd();tcO.scales.y.title.text='Cumulative Taxes ($)';
  var tcDS=[{label:'No Conv',data:D.cumTNo,borderColor:C_NO,borderWidth:2,borderDash:[6,4],tension:0.3,pointRadius:0,fill:false}];
  if(showIP)tcDS.push({label:'Pay from IRA',data:D.cumTIp,borderColor:C_IP,borderWidth:2,tension:0.3,pointRadius:0,fill:false});
  if(outOn)tcDS.push({label:'Pay from Outside',data:D.cumTOp,borderColor:C_OP,borderWidth:2,tension:0.3,pointRadius:0,fill:false});
  charts.push(new Chart($('chartTaxCumul'),{type:'line',data:{labels:D.labels,datasets:tcDS},options:tcO}));
  var taO=cd();taO.scales.y.title.text='Annual Taxes ($)';
  var taDS=[{label:'No Conv',data:D.noTaxY,backgroundColor:'rgba(122,138,154,0.7)',borderRadius:3}];
  if(showIP)taDS.push({label:'Pay from IRA',data:D.ipTaxY,backgroundColor:'rgba(27,138,90,0.6)',borderRadius:3});
  if(outOn)taDS.push({label:'Pay Outside',data:D.opTaxY,backgroundColor:'rgba(0,120,200,0.6)',borderRadius:3});
  charts.push(new Chart($('chartTaxAnnual'),{type:'bar',data:{labels:D.labels,datasets:taDS},options:taO}));
  var iT='Upfront: <span class="hl">'+fmt(taxCost)+'</span>. Lifetime — No conv: <span class="hl">'+fmt(D.cumTNo[fi])+'</span>';
  if(showIP)iT+=', IRA: <span class="hl">'+fmt(D.cumTIp[fi])+'</span>';
  if(outOn)iT+=', Outside: <span class="hl">'+fmt(D.cumTOp[fi])+'</span>';
  $('insightTax').innerHTML='<h4>Tax Analysis</h4><p>'+iT+'.</p>';

  // RMD
  var rS=D.labels.length;for(var rx=0;rx<D.labels.length;rx++){if(D.labels[rx]>=75){rS=rx;break}}
  var rO2=cd();rO2.scales.y.title.text='Annual RMD ($)';
  charts.push(new Chart($('chartRMD'),{type:'bar',data:{labels:D.labels.slice(rS),datasets:[{label:'No Conv RMDs',data:D.noRMD.slice(rS),backgroundColor:'rgba(200,50,50,0.6)',borderRadius:3}]},options:rO2}));
  $('insightRMD').innerHTML='<h4>RMD Details</h4><p>Lifetime: <span class="hl">'+fmt(totRMD)+'</span>. Peak: <span class="hl">'+fmt(pkRMD)+'</span>/yr at age '+pkAge+'. Full conversion eliminates all.</p>';

  // Multi-year
  if(multiYr>1){$('sectionMultiYear').style.display='block';var py=convAmt/multiYr,yL=[],yA=[],yT=[];for(var mi=1;mi<=multiYr;mi++){yL.push('Year '+mi);yA.push(Math.round(py));yT.push(Math.round(py*preTaxR))}var mO=cd();mO.scales.y.title.text='Amount ($)';charts.push(new Chart($('chartMultiYear'),{type:'bar',data:{labels:yL,datasets:[{label:'Conv. Amt',data:yA,backgroundColor:'rgba(0,120,200,0.7)',borderRadius:4},{label:'Tax',data:yT,backgroundColor:'rgba(200,50,50,0.6)',borderRadius:4}]},options:mO}));$('insightMultiYear').innerHTML='<h4>Multi-Year</h4><p>'+fmt(convAmt)+' over '+multiYr+' yrs: <span class="hl">'+fmt(taxCost)+'</span> total tax.</p>'}else{$('sectionMultiYear').style.display='none'}

  // TABLE — No Conversion shows pre+post tax for each account
  // Column count for No Conversion: Trad(pre+post) + RMD Brok(pre+post if on) + Out Brok(pre+post if on) + PreTax Total + AfterTax Total
  var noColCount=4; // trad pre, trad post, pretax total, aftertax total
  if(brokOn)noColCount+=2; // rmd brok pre+post
  if(outOn)noColCount+=2; // out brok pre+post

  var tH='<table><thead><tr><th rowspan="2">Year</th><th rowspan="2">Age</th>';
  tH+='<th colspan="'+noColCount+'" class="gN" style="text-align:center;border-right:2px solid #4DA8DA">── No Conversion ──</th>';
  if(showIP){var ipCols=1+(outOn?1:0)+1;tH+='<th colspan="'+ipCols+'" class="gI" style="text-align:center;border-right:2px solid #4DA8DA">── Pay from IRA ──</th>'}
  if(outOn)tH+='<th colspan="2" class="gO" style="text-align:center">── Pay from Outside ──</th>';
  tH+='</tr><tr>';
  // No Conv sub-headers: pre/post pairs, then totals
  tH+='<th class="gN">Pre Tax<br>Trad. IRA</th><th class="gN">Post Tax<br>Trad. IRA</th>';
  if(brokOn)tH+='<th class="gN">Pre Tax<br>RMD Brok.</th><th class="gN">Post Tax<br>RMD Brok.</th>';
  if(outOn)tH+='<th class="gN">Pre Tax<br>Out. Brok.</th><th class="gN">Post Tax<br>Out. Brok.</th>';
  tH+='<th class="gN">Pre Tax<br>Net Wealth</th><th class="gN" style="border-right:2px solid #4DA8DA">After Tax<br>Net Wealth</th>';
  // Pay from IRA sub-headers
  if(showIP){tH+='<th class="gI">Roth</th>';if(outOn)tH+='<th class="gI">Out. Brok.</th>';tH+='<th class="gI" style="border-right:2px solid #4DA8DA">Net Wealth</th>'}
  // Pay from Outside sub-headers
  if(outOn)tH+='<th class="gO">Roth</th><th class="gO">After Tax<br>Net Wealth</th>';
  tH+='</tr></thead><tbody>';

  for(var ti=0;ti<=fi;ti++){
    var a=D.labels[ti];
    var tr_ti=iraTR(a);
    // After-tax values for each No Conv account
    var noTradAT=Math.round(D.noTrad[ti]*(1-tr_ti));
    var noBrokAT=Math.round(D.noBrok[ti]-Math.max(0,D.noBrok[ti]-D.noBrokB[ti])*cgRate);
    var noOutBAT=outOn?Math.round(D.noOutB[ti]-Math.max(0,D.noOutB[ti]-noOutBB)*cgRate):0;
    // Pre-tax total = sum of gross balances + any roth
    var noPreTot=D.noTrad[ti]+D.noBrok[ti]+(outOn?D.noOutB[ti]:0);

    tH+='<tr><td>'+D.years[ti]+'</td><td>'+a+(a===retAge?' ⭐':'')+(showIP&&a===ipXAge?' 🟢':'')+(outOn&&a===opXAge?' 🔵':'')+(a===75?' 📋':'')+'</td>';
    // No Conv: pre/post pairs
    tH+='<td>'+fK(D.noTrad[ti])+'</td><td>'+fK(noTradAT)+'</td>';
    if(brokOn)tH+='<td>'+fK(D.noBrok[ti])+'</td><td>'+fK(noBrokAT)+'</td>';
    if(outOn)tH+='<td>'+fK(D.noOutB[ti])+'</td><td>'+fK(noOutBAT)+'</td>';
    tH+='<td>'+fK(Math.round(noPreTot))+'</td>';
    tH+='<td style="border-right:2px solid var(--border);font-weight:600">'+fK(D.noW[ti])+'</td>';
    // Pay from IRA (unchanged)
    if(showIP){tH+='<td style="color:var(--success)">'+fK(D.ipRoth[ti])+'</td>';if(outOn)tH+='<td>'+fK(D.ipOutB[ti])+'</td>';tH+='<td style="border-right:2px solid var(--border);font-weight:600">'+fK(D.ipW[ti])+'</td>'}
    // Pay from Outside (unchanged)
    if(outOn)tH+='<td style="color:var(--ms-accent)">'+fK(D.opRoth[ti])+'</td><td style="font-weight:600">'+fK(D.opW[ti])+'</td>';
    tH+='</tr>';
  }
  tH+='</tbody></table>';$('projTable').innerHTML=tH;
  var tn='* Post Tax Trad. IRA = gross × (1 − income tax rate). Post Tax Brokerage = gross − (gains × CG rate). After Tax Net Wealth = sum of all post-tax values. Pre Tax Net Wealth = sum of all gross values.';
  if(brokOn)tn+=' RMD brokerage grows at '+pct(bRet*100)+'.';
  if(outOn)tn+=' Outside brokerage grows at '+pct(outRetRate*100)+'.';
  $('tableNote').textContent=tn;
  $('results').scrollIntoView({behavior:'smooth',block:'start'});
}
</script>
</body>
</html>
